# TaskFlow

This project's instructions live in `AGENTS.md` at the repository root. Read it for the full project layout, development workflow, conventions, and rules.

For the important context in short: this is a monorepo with a Laravel API in `backend/` and a Nuxt 4 client in `frontend/`, run via Docker Compose (`./bin/run.sh`, `./bin/artisan`). See `AGENTS.md` for details.